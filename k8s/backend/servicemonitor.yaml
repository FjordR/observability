apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-sm
  namespace: production
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: backend-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
